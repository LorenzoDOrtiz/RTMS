@page "/workout-history"
@attribute [Authorize]
<PageTitle>Workout History</PageTitle>

@inject UserService UserContextService

<WorkoutHistory UserID="@userId" />

@code {
    private Guid userId;

    protected override void OnInitialized()
    {
        userId = UserContextService.UserId;
    }
}
